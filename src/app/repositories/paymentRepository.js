class PaymentRepository {
    async save(payment) {
        // Lógica para salvar pagamento no banco de dados, se necessário.
        // Retorne o ID do pagamento ou outra informação relevante.
    }
}
module.exports = PaymentRepository;
